{
  "name": "git-es-example",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "babel src/js --out-dir public/js",
    "watch": "babel -w src/js --out-dir public/js",
    "sync": "browser-sync start --config bs-config.js",
    "start": "npm run watch & npm run sync",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.6.2",
    "@babel/core": "^7.6.2",
    "@babel/preset-env": "^7.6.2",
    "babel-watch": "^7.0.0",
    "browser-sync": "^2.27.10"
  },
  "dependencies": {
    "@babel/polyfill": "^7.6.0"
  }
}
